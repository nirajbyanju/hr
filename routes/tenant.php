<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Tenant Routes
|--------------------------------------------------------------------------
|
| Reserved for the database-per-tenant cutover. Intentionally empty for now
| so it cannot clash with the central application's routes (routes/web.php).
| Tenant request routing is wired in a later phase.
|
*/
